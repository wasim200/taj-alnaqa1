[build]
  command = "npm run build"
  ## publish = ".next" # Commented out to let plugin handle it

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  SECRETS_SCAN_ENABLED = "false"

[context.production]
  environment = { NODE_ENV = "production" }
